# data-team-skill — Genome

<!--
  This file was minted by `system/bin/mitosis`. It carries the blueprint by
  INHERITANCE: the lineage header below points up the chain so any Claude Code
  session can walk root -> project -> here. Do NOT paste the root rules in; reach
  them via the Machine root link. Edit the expression block to keep it true.
-->

## Lineage header

- **Parent dir:** ~/hub2/projects
- **Project root:** ~/hub2/projects/data-team-skill
- **Machine root:** ~/hub2/AGENTS.md

> To accumulate full context, walk: Machine root -> Project root -> this file.
> The constitution, hard rules, and reserved port ranges live in the Machine root.

## Expression block

**IS** — Data-Team Skill (/data-team): the modern data team as one composable, model-selectable Claude Code skill — spine + branches + SRE plane, extracted from AiNa

**OWNS** — TODO — nothing yet
<!-- ports / services / data this cell is the registered owner of. Any port here
     must also have a row in ~/hub2/system/registries/PORTS.md. -->

**DEPENDS** — TODO
<!-- what this cell needs to function: gateways, sibling cells, models (via the one
     gateway, never a model port directly). -->

**NEVER** — violate any of the eight hard rules in the machine root
<!-- the hard boundaries for this cell, in addition to the eight rules in the root. -->

**VERIFY** — TODO — the command(s) that prove this cell is healthy/live
<!-- how to confirm this cell is healthy/live: the concrete command(s) or checks a future
     session runs to prove it works (e.g. a curl /health, a CLI smoke test). hub2 verifies
     live; a cell should say how. (Pattern adopted from Agent Zero's DOX "Verification".) -->

**SIBLINGS** — _example, ai-native-analytics, aria, aria-desktop-web, command-center, consensus, demo-gateway, desk-worker, dispatcher, forge, foundry, grok-bridge, hub-dashboard, image-gen, memory, mirror-os, summarizer, switchboard, video-gen
<!-- peer cells under the same parent, for context when navigating. -->
